<div class="center jumbotron">
  <span class = "jumbotron_header"><h1>Roads well traveled...</h1></span>
  <%= button_to 'Begin your road trip', new_road_trip_path, class: "jumbotron_button" %>
</div>
<div class="container-fluid">

  <% @road_trips.each do |road_trip| %>
      <p><%= road_trip.title %></p>
      <p><%= link_to 'View trip', road_trip %></p>
  <% end %>

  <%= link_to 'All profiles', profiles_path %>
</div>
